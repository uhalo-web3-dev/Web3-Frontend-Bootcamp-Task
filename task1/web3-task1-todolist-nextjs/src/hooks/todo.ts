import {useEffect, useState} from "react";
import {ITodoItem} from "@/types/global"

export default function Todo() {
    const [todoCache, setTodoCache] = useState<ITodoItem[]>()
    useEffect(() => {

    }, [])

    return {}
}
